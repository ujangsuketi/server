{
  "name": "backend",
  "version": "1.0.0",
  "description": "Email validation service",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "test": "jest"  // Optional: tambah ini kalau mau test
  },
  "keywords": ["email", "validation", "mx", "dns"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",  // Kalau ini typo, ganti ke "^16.4.5"
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",  // Tambahan: security headers
    "mailcheck": "^1.1.1",  // Tambahan: typo detection
    "node-cache": "^5.1.2",
    "nodemailer": "^6.10.0",
    "validator": "^13.15.15",  // Tambahan: sanitize input
    "winston": "^3.17.0"  // Tambahan: logging
  },
  "devDependencies": {
    "jest": "^29.7.0"  // Optional: buat unit test
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
